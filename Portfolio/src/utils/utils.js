export const textColorPrimaryFunction = (isActive) => {
  return isActive ? "text-darkPrimary" : "text-whitePrimay";
};

export const textColorSecondaryFunction = (isActive) => {
  return isActive ? "text-darkSecondary" : "text-whiteSecondary";
};
